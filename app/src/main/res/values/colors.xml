<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Launch window background (the colour the window and, on API 31+, the system
      splash screen paint before Compose draws its first frame). Must match the
      Compose light background: ui/theme/Color.kt Parchment, used as `background` in
      ui/theme/Theme.kt's LightScheme. Keep the two in sync.
    -->
    <color name="launch_background">#F7F4EE</color>
</resources>
